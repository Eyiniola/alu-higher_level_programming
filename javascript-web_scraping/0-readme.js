#!/usr/bin/node
const fs = require('fs').promises;

const filePath = process.argv[2]; 
const fs.readFileSync(filepath, "utf-8"
function{
	if(err){
		console.log(err);}
	else{
		console.log(line);}
});

